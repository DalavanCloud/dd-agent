init_config:

instances:
  - host: localhost  # hostname used to connect to the kestrel instance
  #   socket: /socket/path # alternative to host; 'dd-agent' user must have read/write permission
  #   port: 22134 # If this line is not present, port will default to 22134
  #   tags:
  #     - optional:tag
